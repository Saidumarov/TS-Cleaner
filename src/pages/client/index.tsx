import { ChakraProvider, Heading } from "@chakra-ui/react";

const Client = () => {
  return (
    <ChakraProvider>
      <div>
        <Heading>Mijoslar</Heading>
      </div>
    </ChakraProvider>
  );
};

export default Client;
