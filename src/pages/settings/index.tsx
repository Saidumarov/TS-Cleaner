import { ChakraProvider, Heading } from "@chakra-ui/react";

const Settings = () => {
  return (
    <ChakraProvider>
      <div>
        <Heading>Sozlamalar</Heading>
      </div>
    </ChakraProvider>
  );
};

export default Settings;
